# django-task-project3

Django + DRF API для управления сборами средств. Реализована ролевая модель: админ может создавать/редактировать, обычный пользователь — только просматривать.

## Быстрый старт

### 1. Клонирование и окружение

```bash
git clone https://github.com/Denis248902/django-task-project3.git
cd django-task-project3
python -m venv venv
source venv/Scripts/activate
pip install -r requirements.txt
